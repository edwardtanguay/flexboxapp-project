export const Box = ({ farbe }) => {
  return (
      <div
        style={{
          backgroundColor: farbe,
          width: "100px",
          height: "100px",
          margin: "10px",
        }}
      >
    </div>
  );
}